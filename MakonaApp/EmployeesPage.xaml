<Page x:Class="MakonaApp.EmployeesPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Сотрудники" Background="#F5E6D3">

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Style="{StaticResource CardStyle}" Padding="15">
            <StackPanel>
                <TextBlock Text="👷 НАША КОМАНДА" FontSize="20" FontWeight="Bold" 
                           Foreground="#C97B5A" HorizontalAlignment="Center"/>
                <TextBlock Text="Сотрудники компании МАКОНА" 
                           FontSize="12" Foreground="#8B6914" HorizontalAlignment="Center" Margin="0,5,0,0"/>
            </StackPanel>
        </Border>

        <Border Grid.Row="1" Style="{StaticResource CardStyle}" Padding="5" Margin="0,15,0,0">
            <DataGrid x:Name="dgEmployees" AutoGenerateColumns="True" IsReadOnly="True"/>
        </Border>
    </Grid>
</Page>