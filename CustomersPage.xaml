<Page x:Class="MakonaApp.CustomersPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Заказчики" Background="#F5E6D3">

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Style="{StaticResource CardStyle}" Padding="15">
            <StackPanel>
                <TextBlock Text="👥 СПИСОК ЗАКАЗЧИКОВ" FontSize="20" FontWeight="Bold" 
                           Foreground="#C97B5A" HorizontalAlignment="Center"/>
                <TextBlock Text="Управление клиентами компании МАКОНА" 
                           FontSize="12" Foreground="#8B6914" HorizontalAlignment="Center" Margin="0,5,0,0"/>
            </StackPanel>
        </Border>

        <Border Grid.Row="1" Style="{StaticResource CardStyle}" Padding="15" Margin="0,15,0,15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="🔍 Поиск:" Grid.Column="0" VerticalAlignment="Center" 
                           FontWeight="Bold" FontSize="14" Margin="0,0,15,0" Foreground="#4A3B2C"/>
                <TextBox x:Name="tbSearch" Grid.Column="1" TextChanged="tbSearch_TextChanged"/>
                <Button Grid.Column="2" Width="30" Height="30" Margin="10,0,0,0"
                        Background="#C97B5A" Foreground="White" BorderThickness="0"
                        Cursor="Hand" Click="ClearSearch_Click">
                    <TextBlock Text="✖" FontSize="14"/>
                </Button>
            </Grid>
        </Border>

        <Border Grid.Row="2" Style="{StaticResource CardStyle}" Padding="5">
            <DataGrid x:Name="dgCustomers" AutoGenerateColumns="True" IsReadOnly="True"/>
        </Border>
    </Grid>
</Page>